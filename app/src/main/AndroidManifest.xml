<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/my_game_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/my_game_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyGameApp"
        tools:targetApi="34">
        <activity
            android:name=".view.timeline.TimelineDetailActivity"
            android:exported="false" />
        <activity
            android:name=".view.SplashScreenActivity"
            android:exported="true"
            android:theme="@style/splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.profile.EditProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.MyGameApp" />
        <activity
            android:name=".view.gamedetail.GameDetailActivity"
            android:exported="false"
            android:theme="@style/Base.Theme.MyGameApp.LightStatusBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".view.allgames.AllGamesActivity"
            android:exported="false" />
        <activity
            android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>